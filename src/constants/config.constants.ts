export const timer = {
  loading: 10000,
};

export const messageTime = 3000;

export const message = {
  common: {
    ok: 'Ok',
    accept: "Accept",
    cancel: "Cancel",
    loading: 'Loading...',
  },

  error: {
    messageErrorServer: 'Se produjo un error accediendo a sus datos.',
  },
  vehicles: {
    confirmDelete: 'Are you sure you want to delete this vehicle?'
  }
};

export const popoverStyles = {
  default: '',
};

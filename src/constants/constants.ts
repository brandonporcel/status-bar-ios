import { routes } from './url.constants';
import { endpoints } from './endpoints.constants';
import { errors } from './errors.constants';
import { storage } from './storage.constants';
import { messages } from './messages';
import { permissions } from './permissions.contants';
import {
  timer,
  messageTime,
  message,
  popoverStyles
} from './config.constants';

export const constants = {
  routes,
  endpoints,
  errors,
  storage,
  messages,
  permissions,
  timer,
  messageTime,
  message,
  popoverStyles
};
